/*
ranging serwerowy top combo

*/

module.exports = {
    name: "leaderboard",

    execute: async(message,args,client) => {
        //foreach plik w foldeże db/flags => wczytaj db.all() 
        //for each users dodaj do zmiennej jego najwyższy wynk i ID
        //po zakończemiu pentli posortuj od najwyższej liczby do najniższej
        //zapisaywać tak: zmienna: { [userconbo,userId]. [user2combo, userId]}
    }
}